def lengthOfLastWord(self, s: str) -> int:
    l,n=0,0
    s+=' '
    for i in range(len(s)):
        if s[i]!= ' ':
            n+=1
        else:
            if n > 0:
                l=n
            n=0
    print(l)
    return l

lengthOfLastWord(self= True, s="My name is Leonard")






